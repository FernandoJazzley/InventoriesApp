import { createTheme } from "@mui/material";

export const inventoriesTheme = createTheme({
    palette: {
        primary: {
            main: '#CACACA'
        },
        secondary: {
            main: '#403C3D'
        },
        error: {
            main:'#590F15'
        }
    }
})